interface:
  display_name: "CC Handoff"
  short_description: "Extract Claude Code chat and tool history"
  default_prompt: "Use $cc-handoff to extract and summarize Claude Code conversation history for this project."
